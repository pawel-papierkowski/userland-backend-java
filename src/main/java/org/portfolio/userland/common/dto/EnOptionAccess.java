package org.portfolio.userland.common.dto;

/**
 * Access rules for option.
 */
public enum EnOptionAccess {
  /** Option is invisible. */
  INVISIBLE,
  /** Option is visible, but disabled. */
  DISABLED,
  /** Option is visible and enabled. */
  ENABLED
}
