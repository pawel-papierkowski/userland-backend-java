logging:
  level:
    # prevent SpringDoc warnings about being enabled by default. For this project we actually want Swagger to be available publicly.
    org.springdoc.core.events.SpringDocAppInitializer: ERROR
