# Configuration specific to UserLand project: GCP config for tests.
# Note GCP is not used in tests.

app:
  gcp:
    general: # general GCP config
      url: "" # The public URL of your Cloud Run service.
      service-account: "" # Your service account. Ensure it has all needed permissions.
      project: "" # project ID
      location: "" # location (region)
    email:
      task: false # If true, use GCP Cloud Task for queueing emails.
      queue: "" # task queue ID for emails
